
<%- include('partials/header') %>
<div class="main">
    <% text.forEach(function(t) { %>
        <p class="hex">
            <% t.split("").forEach(function(c) { %><span class="dark">&zwj;<%=c%></span><%})%>
        </p>
    <% }) %>
</div>
<%- include('partials/footer') %>